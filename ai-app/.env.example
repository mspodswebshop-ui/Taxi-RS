# Je Claude API-sleutel. Haal er een op via https://console.anthropic.com
# LET OP: zet je echte sleutel in .env (die staat in .gitignore), nooit hier.
ANTHROPIC_API_KEY=sk-ant-...

# Poort waarop de app draait (optioneel)
PORT=3000
