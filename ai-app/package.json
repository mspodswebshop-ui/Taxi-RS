{
  "name": "eigen-ai-app",
  "version": "1.0.0",
  "private": true,
  "description": "Eigen AI chat-app op basis van de Claude API",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "node build-standalone.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "dotenv": "^17.0.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
